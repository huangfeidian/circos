#include "../setting/track.h"
#include "../setting/circle.h"
#include "../setting/arc.h"
#include "../setting/glyph.h"
#include "../setting/band.h"
#include <sstream>
#include <string>
#include <vector>
namespace circos
{
	void draw(std::stringstream& output, const circle_setting& on_circle)
	{

	}
	void draw(std::stringstream& output, const circle_setting& on_circle, const std::vector<band_setting>& bands)
	{

	}
	void draw(std::stringstream& output, const circle_setting& on_circle, const std::vector<arc_setting>& arcs)
	{

	}
	void draw(std::stringstream& output, const circle_setting& on_circle, const std::vector<track_setting>& track)
	{

	}
	void draw(std::stringstream& output,const circle_setting& on_cicle,const )
}